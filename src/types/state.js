/**
 * Object that describe root page `/` section state with template `T` refer to used section data type
 * @template [T=any]
 * @typedef {Object} SectionState
 * @property {T} data
 * Refer to an react state that store section data
 * - Initial State : `null`
 * @property {boolean} loading
 * Refer to an react state that flag are section in `loading` state
 * - Initial State : `true`
 * @property {boolean} error
 * Refer to an react state that flag are section in `error` state
 * - Initial State : `false`
*/

export const States = {};